import { TextSize } from '@ui-kits/Typography/typography-types';

export const inputSizeConfig = {
  height: 52,
  borderRadius: 12,
  paddingHorizontal: 16,
  fontSize: 16 as TextSize,
  labelSize: 14 as TextSize,
  errorSize: 12 as TextSize,
  borderWidth: 1,
};

export const defaultPhoneMask = '+9 (999) 999-99-99';